# SCT_CS_4
“The Safe Input Logger is a simple Python program that records only the text a user intentionally enters. Unlike malicious keyloggers, it does not monitor system-wide keystrokes. It provides a safe, beginner-friendly way to learn about logging, input handling, and basic data storage in a controlled environment.”
